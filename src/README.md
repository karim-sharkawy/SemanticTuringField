# `core/`
This is the actual STF simulation. These are all the pieces of the physics engine (the forces, and adding them together)

# `nlp/`
Everything involving language

# `visualization/`
Anything related to drawing. `clustering.py` is here because all it does is color the visualization using KNN, not really anything related to NLP.

# `utils`
Config and helper functions